[phases.setup]
nixPkgs = ["nodejs"]

[phases.install]
cmds = [
  "cd client && npm ci",
  "cd server && npm ci"
]

[phases.build]
cmds = [
  "cd client && npm run build"
]

[start]
cmd = "cd server && node index.js"
